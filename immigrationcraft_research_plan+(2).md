# ImmigrationCraft 研究方案

> UIST 2025 投稿计划 | 最后更新：2024年12月30日

---

## 一、核心问题与定位

### 1.1 研究背景

移民律师的工作流程：

```
Raw Materials → 分类整理 → 高亮关键位置 → 组合成Exhibit → 撰写Petition
     ↑              ↑                                        ↑
   最耗时         需要专业判断                            AI最容易做
```

**现状：**
- 现有AI工具（如Parley、ChatGPT）聚焦于"撰写"环节
- 律师必须人工review所有AI输出
- 但AI是黑盒，律师无法知道"AI基于什么写的"
- Review过程低效，且容易遗漏错误

**核心洞察：**
> 律师不信任AI做材料整理，但AI写作的结果又依赖材料。如果律师无法追溯AI的推理来源，review就变成重新做一遍。

### 1.2 研究问题

**主研究问题：**
> 当AI生成的法律文本具有可追溯性（每个声明可链接到源材料的具体位置），律师的review工作流程会如何改变？

**子问题：**
- RQ1: 可追溯性是否帮助律师更高效地review AI输出？
- RQ2: 可追溯性是否提高律师对AI输出的信心？
- RQ3: 律师如何使用追溯功能？有哪些使用模式？

### 1.3 核心贡献

| 贡献类型 | 内容 |
|----------|------|
| **系统贡献** | ImmigrationCraft：一个可追溯的AI移民申请写作系统 |
| **技术贡献** | Multi-stage Semantic Compression：语义压缩+溯源保留的处理架构 |
| **交互贡献** | Inline Provenance + Gap Dashboard：将AI输出链接到源材料具体位置，支持交互式证据探索 |
| **实证贡献** | 证明可追溯性提高律师review效率和信心 |

### 1.4 与竞品的差异化

| 维度 | Parley | ChatGPT | ImmigrationCraft |
|------|--------|---------|------------------|
| 定位 | 效率优先 | 通用写作 | 可验证性优先 |
| 领域知识 | ✅ 有 | ❌ 无 | ✅ 有 |
| 证据追溯 | ❌ 无 | ❌ 无 | ✅ **有** |
| Gap分析 | ❌ 无 | ❌ 无 | ✅ **有** |
| 源材料定位 | ❌ 无 | ❌ 无 | ✅ **精确到位置** |

**一句话定位：**
> "Parley帮律师写得更快。ImmigrationCraft帮律师验证得更可靠。"

---

## 二、系统设计

### 2.1 系统架构：多阶段Pipeline

**设计理念：** 
传统RAG方法在chunking时丢失文档完整语义。我们采用"语义压缩 + 溯源保留"策略：AI先理解全文语义，压缩为关键信息，同时保留到原文的精确坐标映射。后续阶段基于压缩信息生成，但溯源指向原始文档位置。

```
┌─────────────────────────────────────────────────────────────────┐
│  阶段0：文档处理 (OCR)                                           │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  上传PDF → DeepSeek OCR                                    │  │
│  │                 ↓                                          │  │
│  │  输出两个结果：                                             │  │
│  │  • BBox坐标数据 ──→ 存储，供后续UI高亮回溯                  │  │
│  │  • Markdown文本 ──→ 进入阶段1                              │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                                  ↓
┌─────────────────────────────────────────────────────────────────┐
│  阶段1：语义分析 + 信息压缩                                      │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  输入：完整Markdown文本                                     │  │
│  │                                                            │  │
│  │  AI任务：                                                  │  │
│  │  • 理解文档完整语义上下文                                   │  │
│  │  • 识别与L-1法律标准相关的关键信息                          │  │
│  │  • 压缩成结构化摘要，保留关键事实                           │  │
│  │  • 每个摘要点 mapping 到原文位置 → 关联BBox                 │  │
│  │                                                            │  │
│  │  输出：压缩后的关键信息 + 溯源坐标                          │  │
│  │                                                            │  │
│  │  💡 律师介入点：可通过拖动等可视化方式调整AI识别的关键信息   │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                                  ↓
┌─────────────────────────────────────────────────────────────────┐
│  阶段2：关键引用 (Evidence-Standard Mapping)                     │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  输入：所有文档的压缩信息                                   │  │
│  │                                                            │  │
│  │  AI任务：                                                  │  │
│  │  • 跨文档整合：哪些证据支持哪个L-1要素？                    │  │
│  │  • 建立 Evidence → Legal Standard 映射                     │  │
│  │    - Specialized Knowledge: doc_1, doc_3                   │  │
│  │    - Managerial Capacity: doc_2, doc_4                     │  │
│  │    - Company Relationship: doc_5                           │  │
│  │                                                            │  │
│  │  输出：结构化证据清单（带溯源）                              │  │
│  │                                                            │  │
│  │  💡 律师介入点：可调整证据-标准映射关系                      │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                                  ↓
┌─────────────────────────────────────────────────────────────────┐
│  阶段3：逻辑分析 (Gap Analysis)                                  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  输入：证据清单 + L-1法律标准要求                           │  │
│  │                                                            │  │
│  │  AI任务：                                                  │  │
│  │  • Gap分析：哪些要素证据充足？哪些薄弱？                    │  │
│  │  • 论证路径规划：怎么串联证据最有说服力？                   │  │
│  │  • 补充建议：缺什么材料？                                   │  │
│  │                                                            │  │
│  │  输出：论证大纲 + Gap报告 + What-if预览                     │  │
│  │                                                            │  │
│  │  💡 律师介入点：Gap Dashboard交互式探索                     │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                                  ↓
┌─────────────────────────────────────────────────────────────────┐
│  阶段4：Petition写作                                            │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  输入：论证大纲 + 压缩信息（带溯源坐标）                     │  │
│  │                                                            │  │
│  │  AI任务：                                                  │  │
│  │  • 按大纲生成petition各段落                                 │  │
│  │  • 每个claim附带citation → 指向原文BBox                    │  │
│  │                                                            │  │
│  │  输出：Petition草稿 + Inline Provenance                     │  │
│  │                                                            │  │
│  │  💡 律师介入点：编辑文本，点击溯源验证                       │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

**Pipeline设计优势：**

| 传统方法 | ImmigrationCraft Pipeline |
|----------|---------------------------|
| 全文直接生成，无法追溯 | 分阶段处理，每步可追溯 |
| Chunking丢失上下文 | 语义压缩保留完整理解 |
| 律师只能在最后review | 律师可在任意阶段介入调整 |
| 引用粒度粗（文档级） | 引用精确到原文坐标（BBox级） |

**律师介入模式：**
```
AI先出基础结果 → 律师通过可视化交互调整（拖动、确认、修改）→ 进入下一阶段
```
- 默认自动流转，律师可选择性介入
- 介入方式：直观的可视化操作（拖动、高亮、确认），而非文字编辑
- 每个阶段的AI输出都可被律师覆盖或调整

### 2.2 核心创新点

#### 创新点1：Multi-stage Semantic Compression with Provenance Preservation

**问题：** 
原始文档太长无法全部输入LLM。传统RAG的chunking方法会丢失文档完整语义，且引用不精确。

**解决方案：**
```
原始文档 (长，如30页简历+支持信)
       ↓
AI理解完整语义（阶段1）
       ↓
压缩成关键信息 (短) + 保留溯源坐标
       ↓
后续阶段用压缩版生成，但引用指向原文精确位置
```

**技术特点：**
- **有损压缩**：信息量减少，但保留法律相关的关键事实
- **溯源无损**：每个压缩后的信息点都mapping到原文BBox坐标
- **语义完整**：压缩前AI已理解全文上下文，避免chunking的断章取义

**论文包装：**
> "Unlike RAG approaches that chunk documents before understanding, our semantic compression first comprehends the full document context, then extracts legally-relevant key facts while preserving precise source coordinates for provenance."

#### 创新点2：Inline Provenance

**功能：** 每个AI生成的声明都可以点击，跳转到源材料的具体位置

```
Petition文本：
"申请人于2019年3月加入ABC公司担任高级工程师[1]，
 负责领导一个12人的技术团队[2]..."

点击[1] → 跳转到: employment_letter.pdf 第2页，高亮相关段落
点击[2] → 跳转到: org_chart.pdf 第1页，高亮team structure部分
```

**技术实现：**
- OCR提取文本 + Bounding Box位置信息
- LLM生成时记录source reference
- 前端实现PDF定位和高亮

**技术难点与贡献：**

**1. Many-to-One Citation（多源引用）**
```
问题：LLM生成的一句话可能综合多处证据
例如："申请人在ABC项目中运用了Python技能"
     → "ABC项目"来自第3页，"Python"来自第1页简历

解决：支持一个句子对应多个引用标号 [1][2]
     点击不同标号跳转到不同位置
```

**2. 结构化图表溯源（待验证 + Fallback）**
```
问题：Org Chart等图表不是纯文本，传统OCR无法处理
例如："申请人管理5人团队" → 来自Org Chart的树状结构

解决方案A（自动）：
利用DeepSeek OCR识别图表结构
将生成文本链接到图表的具体区域

解决方案B（Fallback - Human-in-the-Loop）：
当系统无法自动识别时，律师可以手动框选图表区域
并添加简短注释，系统记录该区域作为证据来源

⚠️ Week 1实验验证自动识别效果
   - 效果好 → 主打方案A
   - 效果差 → 主打方案B，包装为Human-AI Collaboration
```

#### 创新点3：Evidence Gap Dashboard（交互式）

**功能：** 可视化证据缺口 + 补充建议 + 假设性预览

**核心交互：What-if Exploration**

律师不仅能看到"缺什么"，还能预览"补了之后petition会怎么写"

```
┌─────────────────────────────────────────────────────────────┐
│  L-1B Evidence Coverage                                      │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ✅ Specialized Knowledge          ■■■■■■■■□□  80%          │
│     3 documents supporting                                   │
│                                                              │
│  ⚠️ Company Relationship           ■■■■■□□□□□  50%          │
│     2 documents supporting                                   │
│     [查看建议] [预览补充后效果]                               │
│                                                              │
│  ❌ Qualifying Position            ■■□□□□□□□□  20%          │
│     1 document supporting                                    │
│     [查看建议] [预览补充后效果]                               │
│                                                              │
└─────────────────────────────────────────────────────────────┘

点击 [查看建议]:
┌─────────────────────────────────────────────────────────────┐
│  Qualifying Position - 建议补充材料                          │
├─────────────────────────────────────────────────────────────┤
│  当前问题：缺少证明申请人在美国职位具有管理/专业性质的证据      │
│                                                              │
│  建议补充：                                                   │
│  • 组织架构图 showing reporting structure                    │
│  • HR出具的职位描述信                                        │
│  • 项目文档 showing specialized responsibilities             │
│                                                              │
│  [预览：如果补充这些材料，petition相关段落会如何改写]          │
└─────────────────────────────────────────────────────────────┘

点击 [预览补充后效果]:
┌─────────────────────────────────────────────────────────────┐
│  假设性预览 - Qualifying Position段落                        │
├─────────────────────────────────────────────────────────────┤
│  当前版本：                                                   │
│  "申请人担任高级工程师职位，负责技术开发工作。"                │
│                                                              │
│  ────────────────────────────────────────                   │
│                                                              │
│  补充后版本（假设性）：                                       │
│  "申请人担任高级工程师职位，直接向CTO汇报[组织架构图]，        │
│   管理一个5人技术团队[HR信]，负责领导公司核心产品的            │
│   架构设计和技术决策[项目文档]..."                            │
│                                                              │
│  ⚠️ 此为假设性预览，需要客户补充实际材料后重新生成            │
└─────────────────────────────────────────────────────────────┘
```

**交互价值：**
- 帮助律师决定"要不要让客户补材料"
- 让律师看到"补了之后能写多好"
- 减少来回沟通成本

**技术实现：**
- 定义每种签证类型的Legal Standards及其证据要求
- LLM判断每份材料支持哪些standards
- LLM生成针对性的补充建议
- LLM生成假设性petition段落预览（使用placeholder证据）

### 2.3 技术栈

| 组件 | 技术选择 | 说明 |
|------|----------|------|
| **OCR** | DeepSeek OCR | 语义分析 + 关键坐标溯源 |
| **LLM** | 开源模型（Qwen2.5 / Llama3 / DeepSeek） | 本地部署，保护隐私 |
| **前端** | React + PDF.js | 交互设计灵活，PDF高亮跳转成熟 |
| **后端** | FastAPI (Python) | 快速响应，与LLM生态兼容 |
| **数据库** | SQLite / PostgreSQL | 存储案例、证据链、用户数据 |
| **部署** | Docker + 本地服务器 | 满足律所数据隐私要求 |

#### 关键技术实现

**1. PDF高亮跳转（Inline Provenance）**
```javascript
// PDF.js实现点击跳转+高亮
// 存储结构：{ page_number, bbox: {x, y, width, height} }
onClick(citation) {
  const { page, bbox } = citation.source_location;
  pdfViewer.scrollToPage(page);
  pdfViewer.highlightArea(bbox);
}
```

**2. Gap预览生成（需要快速响应）**
```python
# FastAPI接口
@app.post("/preview_with_gap")
async def preview(standard: str, suggested_docs: list):
    prompt = f"假设补充了{suggested_docs}，重写{standard}段落..."
    return await llm.generate(prompt)
```

**性能建议：** 使用量化模型（如Qwen2.5-7B-GPTQ）确保本地推理速度

### 2.4 具体实现：OCR与回溯系统

本节详细描述阶段0（OCR）和回溯系统的技术实现，作为开发的第一优先级。

---

#### 2.4.1 阶段0：OCR处理

**目标：** 将上传的PDF转换为结构化数据，同时保留精确的位置信息用于后续回溯。

**技术选型：DeepSeek OCR API**

```python
# 调用示例
import requests
import base64

def process_document(pdf_path: str) -> dict:
    """
    处理单个PDF文档，返回OCR结果
    
    Returns:
        {
            "markdown": str,           # 完整文本（Markdown格式）
            "pages": [                 # 按页组织的详细信息
                {
                    "page_number": 1,
                    "blocks": [        # 文本块列表
                        {
                            "text": "Led a team of 5 engineers...",
                            "bbox": {
                                "x": 72,      # 左上角x（单位：pt）
                                "y": 120,     # 左上角y
                                "width": 468,
                                "height": 24
                            },
                            "block_id": "p1_b0",  # 唯一标识
                            "type": "paragraph"   # paragraph|heading|table|image
                        },
                        ...
                    ]
                },
                ...
            ],
            "metadata": {
                "total_pages": 3,
                "document_type": "resume",  # 可选：AI识别的文档类型
                "processing_time": 2.3
            }
        }
    """
    with open(pdf_path, "rb") as f:
        pdf_base64 = base64.b64encode(f.read()).decode()
    
    response = requests.post(
        "https://api.deepseek.com/v1/ocr",  # 示例endpoint
        json={
            "file": pdf_base64,
            "output_format": "markdown_with_positions",
            "detect_tables": True,
            "detect_images": True
        },
        headers={"Authorization": f"Bearer {API_KEY}"}
    )
    
    return response.json()
```

**输出数据结构设计：**

```python
from pydantic import BaseModel
from typing import List, Optional

class BoundingBox(BaseModel):
    x: float          # 左上角x坐标 (pt)
    y: float          # 左上角y坐标 (pt)
    width: float      # 宽度 (pt)
    height: float     # 高度 (pt)

class TextBlock(BaseModel):
    block_id: str           # 唯一标识，格式: "p{page}_b{block}"
    text: str               # 文本内容
    bbox: BoundingBox       # 位置信息
    block_type: str         # paragraph | heading | table | list_item | image_caption
    confidence: float       # OCR置信度 (0-1)

class PageContent(BaseModel):
    page_number: int
    width: float            # 页面宽度 (pt)
    height: float           # 页面高度 (pt)
    blocks: List[TextBlock]

class DocumentOCRResult(BaseModel):
    document_id: str
    filename: str
    markdown: str                    # 完整Markdown文本
    pages: List[PageContent]         # 带位置的详细内容
    total_pages: int
    processing_time: float
```

**数据库存储设计：**

```sql
-- 文档表
CREATE TABLE documents (
    id UUID PRIMARY KEY,
    case_id UUID REFERENCES cases(id),
    filename VARCHAR(255),
    upload_time TIMESTAMP,
    markdown_content TEXT,          -- 完整Markdown
    total_pages INT,
    document_type VARCHAR(50),      -- resume, support_letter, org_chart, etc.
    processing_status VARCHAR(20)   -- pending, processing, completed, failed
);

-- 文本块表（用于精确回溯）
CREATE TABLE text_blocks (
    id UUID PRIMARY KEY,
    document_id UUID REFERENCES documents(id),
    block_id VARCHAR(50),           -- p1_b0 格式
    page_number INT,
    text_content TEXT,
    bbox_x FLOAT,
    bbox_y FLOAT,
    bbox_width FLOAT,
    bbox_height FLOAT,
    block_type VARCHAR(20),
    confidence FLOAT,
    
    -- 索引：支持按文档和页码快速查询
    INDEX idx_doc_page (document_id, page_number)
);

-- 后续阶段使用：关键信息表（阶段1输出）
CREATE TABLE key_facts (
    id UUID PRIMARY KEY,
    document_id UUID REFERENCES documents(id),
    fact_text TEXT,                 -- 压缩后的关键信息
    source_block_ids TEXT[],        -- 关联的原文block_id列表
    legal_category VARCHAR(50),     -- specialized_knowledge, managerial_capacity, etc.
    ai_confidence FLOAT,
    lawyer_confirmed BOOLEAN DEFAULT FALSE
);
```

---

#### 2.4.2 回溯系统实现

**目标：** 用户点击生成文本中的引用标记时，PDF阅读器跳转到对应位置并高亮。

**前端架构：**

```
┌─────────────────────────────────────────────────────────────┐
│  ImmigrationCraft UI                                         │
│  ┌─────────────────────────┐  ┌─────────────────────────┐   │
│  │   Petition Editor       │  │   PDF Viewer            │   │
│  │                         │  │   (react-pdf + PDF.js)  │   │
│  │   "申请人管理5人团队[1]  │  │                         │   │
│  │    负责核心系统开发[2]"  │──→│   ┌─────────────────┐  │   │
│  │                         │  │   │ █████████████████ │  │   │
│  │   点击[1]               │  │   │ (高亮区域)        │  │   │
│  │                         │  │   └─────────────────┘  │   │
│  └─────────────────────────┘  └─────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

**React组件结构：**

```typescript
// types.ts
interface Citation {
  id: string;
  displayIndex: number;        // 显示的引用编号 [1], [2]
  sourceBlockIds: string[];    // 可能引用多个block（Many-to-One）
  documentId: string;
  documentName: string;
}

interface HighlightTarget {
  documentId: string;
  pageNumber: number;
  bbox: {
    x: number;
    y: number;
    width: number;
    height: number;
  };
}

// CitationLink.tsx - 可点击的引用标记
interface CitationLinkProps {
  citation: Citation;
  onCitationClick: (targets: HighlightTarget[]) => void;
}

const CitationLink: React.FC<CitationLinkProps> = ({ citation, onCitationClick }) => {
  const handleClick = async () => {
    // 根据block_ids查询对应的位置信息
    const targets = await fetchHighlightTargets(citation.sourceBlockIds);
    onCitationClick(targets);
  };

  return (
    <span 
      className="citation-link"
      onClick={handleClick}
      title={`来源: ${citation.documentName}`}
    >
      [{citation.displayIndex}]
    </span>
  );
};
```

**PDF高亮跳转实现：**

```typescript
// PDFViewerWithHighlight.tsx
import { Document, Page, pdfjs } from 'react-pdf';
import { useState, useRef, useEffect } from 'react';

interface PDFViewerProps {
  documentUrl: string;
  highlightTargets: HighlightTarget[];  // 当前需要高亮的区域
}

const PDFViewerWithHighlight: React.FC<PDFViewerProps> = ({ 
  documentUrl, 
  highlightTargets 
}) => {
  const [numPages, setNumPages] = useState<number>(0);
  const [currentPage, setCurrentPage] = useState<number>(1);
  const [scale, setScale] = useState<number>(1.0);
  const containerRef = useRef<HTMLDivElement>(null);

  // 当highlightTargets变化时，跳转到第一个目标
  useEffect(() => {
    if (highlightTargets.length > 0) {
      const firstTarget = highlightTargets[0];
      scrollToHighlight(firstTarget);
    }
  }, [highlightTargets]);

  const scrollToHighlight = (target: HighlightTarget) => {
    setCurrentPage(target.pageNumber);
    
    // 等待页面渲染后滚动到具体位置
    setTimeout(() => {
      const highlightElement = document.getElementById(
        `highlight-${target.pageNumber}-${target.bbox.y}`
      );
      highlightElement?.scrollIntoView({ behavior: 'smooth', block: 'center' });
    }, 100);
  };

  // 渲染高亮覆盖层
  const renderHighlightOverlay = (pageNumber: number, pageWidth: number, pageHeight: number) => {
    const pageHighlights = highlightTargets.filter(t => t.pageNumber === pageNumber);
    
    return pageHighlights.map((target, idx) => {
      // PDF坐标转换为屏幕坐标
      // PDF坐标原点在左下角，需要转换
      const screenX = (target.bbox.x / 72) * 96 * scale;  // pt to px
      const screenY = ((pageHeight - target.bbox.y - target.bbox.height) / 72) * 96 * scale;
      const screenWidth = (target.bbox.width / 72) * 96 * scale;
      const screenHeight = (target.bbox.height / 72) * 96 * scale;

      return (
        <div
          key={`${pageNumber}-${idx}`}
          id={`highlight-${pageNumber}-${target.bbox.y}`}
          className="highlight-overlay"
          style={{
            position: 'absolute',
            left: screenX,
            top: screenY,
            width: screenWidth,
            height: screenHeight,
            backgroundColor: 'rgba(255, 230, 0, 0.4)',
            border: '2px solid rgba(255, 180, 0, 0.8)',
            borderRadius: '2px',
            pointerEvents: 'none',
            transition: 'opacity 0.3s ease',
          }}
        />
      );
    });
  };

  return (
    <div ref={containerRef} className="pdf-viewer-container">
      <Document
        file={documentUrl}
        onLoadSuccess={({ numPages }) => setNumPages(numPages)}
      >
        {Array.from({ length: numPages }, (_, i) => i + 1).map(pageNum => (
          <div key={pageNum} style={{ position: 'relative' }}>
            <Page
              pageNumber={pageNum}
              scale={scale}
              renderTextLayer={true}
              renderAnnotationLayer={false}
            />
            {/* 高亮覆盖层 */}
            <div className="highlight-layer" style={{ position: 'absolute', top: 0, left: 0 }}>
              {renderHighlightOverlay(pageNum, 612, 792)}  {/* Letter size */}
            </div>
          </div>
        ))}
      </Document>
    </div>
  );
};
```

**后端API设计：**

```python
# api/routes/documents.py
from fastapi import APIRouter, UploadFile, File, HTTPException
from typing import List

router = APIRouter(prefix="/api/documents", tags=["documents"])

@router.post("/upload")
async def upload_document(
    case_id: str,
    file: UploadFile = File(...)
) -> dict:
    """
    上传文档并触发OCR处理
    
    Returns:
        {
            "document_id": "uuid",
            "status": "processing",
            "message": "Document uploaded, OCR processing started"
        }
    """
    # 1. 保存文件
    file_path = save_uploaded_file(file, case_id)
    
    # 2. 创建文档记录
    document_id = create_document_record(case_id, file.filename)
    
    # 3. 异步启动OCR处理
    background_tasks.add_task(process_ocr, document_id, file_path)
    
    return {
        "document_id": document_id,
        "status": "processing"
    }

@router.get("/{document_id}/ocr-status")
async def get_ocr_status(document_id: str) -> dict:
    """检查OCR处理状态"""
    doc = get_document(document_id)
    return {
        "status": doc.processing_status,
        "progress": doc.processing_progress  # 0-100
    }

@router.get("/{document_id}/blocks")
async def get_document_blocks(
    document_id: str,
    page: int = None
) -> List[dict]:
    """
    获取文档的文本块信息（用于回溯高亮）
    
    可选按页码过滤
    """
    blocks = get_blocks_by_document(document_id, page)
    return [
        {
            "block_id": b.block_id,
            "page_number": b.page_number,
            "text": b.text_content[:100] + "..." if len(b.text_content) > 100 else b.text_content,
            "bbox": {
                "x": b.bbox_x,
                "y": b.bbox_y,
                "width": b.bbox_width,
                "height": b.bbox_height
            }
        }
        for b in blocks
    ]

@router.post("/blocks/highlight-targets")
async def get_highlight_targets(
    block_ids: List[str]
) -> List[dict]:
    """
    根据block_id列表返回高亮目标位置
    
    用于点击citation时获取跳转位置
    """
    blocks = get_blocks_by_ids(block_ids)
    return [
        {
            "document_id": b.document_id,
            "document_name": get_document(b.document_id).filename,
            "page_number": b.page_number,
            "bbox": {
                "x": b.bbox_x,
                "y": b.bbox_y,
                "width": b.bbox_width,
                "height": b.bbox_height
            }
        }
        for b in blocks
    ]
```

---

#### 2.4.3 数据流完整示例

```
用户操作                    系统处理                         数据存储
────────────────────────────────────────────────────────────────────────

1. 上传 Resume.pdf    →    保存文件                    →    files/case_001/
                           创建document记录            →    documents表
                           
2. [自动] OCR处理     →    调用DeepSeek OCR API
                           解析返回结果
                           存储Markdown              →    documents.markdown_content
                           存储各block               →    text_blocks表
                           更新状态为completed       →    documents.processing_status

3. [阶段1] 语义分析   →    读取markdown_content
                           LLM提取关键信息
                           mapping到block_ids        →    key_facts表
                           
4. 用户点击[1]引用    →    查询key_facts获取block_ids
                           查询text_blocks获取bbox
                           返回HighlightTarget
                           
5. 前端跳转高亮       →    PDFViewer滚动到页面
                           渲染高亮覆盖层
```

---

#### 2.4.4 开发任务分解

**Phase 1: OCR基础 (Week 1)**

| 任务 | 预计时间 | 产出 |
|------|----------|------|
| DeepSeek OCR API调研 | 2h | API文档笔记，确认输出格式 |
| 数据库Schema设计 | 2h | SQL migration文件 |
| OCR处理服务 | 4h | `services/ocr_processor.py` |
| 文档上传API | 3h | `api/routes/documents.py` |
| 单元测试 | 2h | 测试用例 |

**Phase 2: 前端PDF查看器 (Week 1-2)**

| 任务 | 预计时间 | 产出 |
|------|----------|------|
| react-pdf集成 | 2h | 基础PDF渲染组件 |
| 高亮覆盖层实现 | 4h | `PDFViewerWithHighlight.tsx` |
| 坐标转换调试 | 3h | 不同PDF尺寸的兼容处理 |
| 滚动跳转动画 | 2h | 平滑跳转效果 |

**Phase 3: Citation系统 (Week 2)**

| 任务 | 预计时间 | 产出 |
|------|----------|------|
| Citation数据结构 | 1h | TypeScript类型定义 |
| CitationLink组件 | 2h | 可点击引用标记 |
| 后端查询API | 2h | `/blocks/highlight-targets` |
| 多文档切换 | 3h | 当引用指向不同文档时的处理 |
| 集成测试 | 2h | 端到端测试 |

**验收标准：**
```
□ 上传PDF后3秒内开始OCR处理
□ OCR完成后，text_blocks表有正确的bbox数据
□ 点击citation能跳转到正确页面
□ 高亮区域精确覆盖源文本（误差<5px）
□ 支持同时高亮多个区域（Many-to-One）
□ 页面刷新后高亮状态可恢复
```

---

#### 2.4.5 技术风险与备选方案

| 风险 | 可能性 | 影响 | 应对方案 |
|------|--------|------|----------|
| DeepSeek OCR不返回bbox | 中 | 高 | 备选：pdf2image + Tesseract with hOCR |
| PDF坐标系不一致 | 高 | 中 | 建立坐标转换工具库，覆盖常见PDF类型 |
| 大文件OCR超时 | 中 | 中 | 分页处理 + 进度显示 |
| 扫描件OCR质量差 | 中 | 中 | 前端提示用户上传清晰文档 |

**备选OCR方案：**

```python
# 如果DeepSeek不可用，使用Tesseract + pdf2image
from pdf2image import convert_from_path
import pytesseract

def fallback_ocr(pdf_path: str) -> dict:
    images = convert_from_path(pdf_path, dpi=300)
    result = {"pages": [], "markdown": ""}
    
    for i, image in enumerate(images):
        # 使用hOCR输出获取位置信息
        hocr = pytesseract.image_to_pdf_or_hocr(image, extension='hocr')
        blocks = parse_hocr(hocr)  # 解析hOCR获取bbox
        
        result["pages"].append({
            "page_number": i + 1,
            "blocks": blocks
        })
        
        text = pytesseract.image_to_string(image, lang='eng')
        result["markdown"] += f"\n\n## Page {i+1}\n\n{text}"
    
    return result
```

---

## 三、实验设计

### 3.1 实验概述

| 要素 | 设计 |
|------|------|
| 类型 | **Within-subjects**（组内对比） |
| 参与者 | 6-8名移民律师/paralegal |
| 条件 | Baseline vs Treatment（每人都做两次） |
| 任务 | 使用系统生成并review L-1 petition |
| 顺序平衡 | 一半先Baseline后Treatment，一半反过来 |

### 3.2 案例准备（骨架克隆法）

为保证两个案例认知负荷等价：

```
Case A (软件工程师):
- 文档：1简历 + 1 Org Chart + 1 Support Letter + 2项目文档
- 预埋缺口：无法证明管理职能（Org Chart模糊）

Case B (化工研究员):
- 文档：1简历 + 1 Org Chart + 1 Support Letter + 2项目文档
- 预埋缺口：无法证明独特性知识（项目文档太通用）

控制变量：
- 文档数量严格一致
- 总阅读量差异 ≤10%
- Pilot Study验证耗时相近
```

### 3.3 实验条件

#### 条件A：Baseline（律师 + 通用LLM）

- **工具：** ChatGPT/Claude + 源文档PDF
- **流程：** 
  1. 律师阅读源文档
  2. 用ChatGPT辅助起草petition
  3. Review并修改
- **特点：** 无追溯功能，需手动查找源文档
- **定位：** Current Practice（律师当前真实工作流）

**Baseline设置原则：**
```
要做的：
✓ 给ChatGPT一个decent的prompt（说明任务、提供petition模板）
✓ 让律师可以自由迭代对话
✓ 提供PDF文件让他们在另一个窗口查阅
✓ 模拟律师真实的双屏工作环境

不要做的：
✗ 给ChatGPT超级复杂的prompt让它变强
✗ 过度优化baseline（那是在帮对手）
✗ 使用ChatGPT没有的功能（如Code Interpreter读PDF）

论文中的Frame：
"Baseline代表律师当前使用通用LLM辅助写作的典型工作流，
 而非针对特定任务优化的系统。"
```

#### 条件B：Treatment（律师 + ImmigrationCraft）

- **工具：** ImmigrationCraft完整功能
- **流程：**
  1. 上传源文档
  2. 系统生成petition（带provenance）
  3. Review并修改（可点击追溯）
- **特点：** 有Inline Provenance，有Gap Dashboard

### 3.4 实验流程

```
参与者分组（顺序平衡）：
Group 1 (N/2人): Case A + Baseline → Case B + Treatment
Group 2 (N/2人): Case B + Treatment → Case A + Baseline

单次session流程（约70分钟）：
├── 介绍 + Consent (5分钟)
├── 第一个任务 (25分钟)
│   ├── 系统培训 (3分钟)
│   ├── 生成草稿 (7分钟)
│   └── Review修改 (15分钟)
├── 休息 (5分钟)
├── 第二个任务 (25分钟)
│   ├── 系统培训 (3分钟)
│   ├── 生成草稿 (7分钟)
│   └── Review修改 (15分钟)
├── 问卷 (5分钟)
└── 访谈 (10分钟)
```

### 3.3 实验任务

```
场景：你收到一位L-1B签证申请人的材料，需要准备petition letter。

材料包括：
- Employment verification letter
- Resume/CV
- Organizational chart
- Project documentation
- ...

任务：
1. 使用提供的工具生成petition草稿（10分钟）
2. Review草稿，进行必要的修改（20分钟）
3. 提交你认为可以使用的最终版本

要求：
- 确保所有声明都有证据支持
- 标记任何你认为有问题的地方
- Think aloud：边做边说出你的思考
```

### 3.4 测量指标

#### A. 商业价值指标（满足律所需求）

| 指标 | 测量方式 | 说明 |
|------|----------|------|
| Petition质量 | 专家评分（1-7） | 请资深律师盲评两组输出 |
| 事实准确率 | 对照源文档检查 | 统计错误/不准确声明数量 |
| 完成时间 | 计时 | 从开始到提交的时间 |
| 修改量 | 统计 | 对AI初稿的修改次数和程度 |

#### B. 学术价值指标（满足UIST）

| 指标 | 测量方式 | 说明 |
|------|----------|------|
| Provenance使用 | 日志记录 | 点击次数、停留时间、使用模式 |
| Review行为 | 屏幕录制 | 编码分析review策略 |
| 信心评分 | 问卷 | "你对这份petition的准确性有多确信？"(1-7) |
| 工作负荷 | NASA-TLX | 标准化工作负荷量表 |
| 主观反馈 | 事后访谈 | 15-20分钟半结构化访谈 |

### 3.5 访谈提纲（事后）

1. **总体感受**
   - 使用这个系统的体验如何？
   - 和你平时的工作流程比，有什么不同？

2. **Provenance功能**（仅Treatment组）
   - 你使用了点击追溯功能吗？在什么情况下使用？
   - 这个功能对你的review有帮助吗？怎么帮助的？
   - 有没有想用但是发现不好用的时候？

3. **信任与信心**
   - 你对AI生成的内容有多信任？
   - 什么会让你更信任/不信任？
   - 你觉得还需要什么才能放心使用？

4. **改进建议**
   - 如果可以改进一个地方，你希望改什么？
   - 你会在实际工作中使用这样的工具吗？为什么？

### 3.7 数据分析计划

#### 定量分析（配对比较）

| 比较 | 指标 | 方法 |
|------|------|------|
| Baseline vs Treatment | 完成时间 | **配对t检验** (Paired t-test) |
| Baseline vs Treatment | 准确率 | **配对t检验** (Paired t-test) |
| Baseline vs Treatment | 信心评分 | **Wilcoxon符号秩检验** |
| Baseline vs Treatment | 工作负荷 | **Wilcoxon符号秩检验** |
| 顺序效应检验 | 各指标 | 混合ANOVA (Order × Condition) |

**⚠️ 小样本策略（重要）：**

由于样本量为6-8人，统计显著性（p < .05）可能难以达到。采用以下策略：

```
结果报告方式：

1. 报告效应量（Effect Size）而非依赖p值
   - Cohen's d > 0.8 为大效应
   - 即使p值不显著，大效应量仍有实际意义

2. 结果部分写法示例：
   "Treatment组完成时间更短(M=12.3min, SD=2.1 vs M=18.7min, SD=3.4)，
    效应量较大(Cohen's d=1.2)。信心评分更高(M=5.2 vs M=3.8, d=0.9)。
    由于样本量限制，部分结果未达统计显著(p=.08)，
    但效应量表明存在实际意义的差异。"

3. 定量为辅，定性为主
   - 定量结果简要报告
   - 重点展示律师Quote、行为模式、具体案例
```

#### 定性分析（重点）

- 访谈转录 → 主题分析（Thematic Analysis）
- 屏幕录制 → 行为编码（Behavioral Coding）
- 识别provenance使用模式
- 对比同一律师在两种条件下的行为差异
- **收集有力的Quote**用于论文

### 3.7 实验材料准备

#### 需要准备的案例材料

| 项目 | 数量 | 来源 |
|------|------|------|
| L-1B完整案例 | 2-3个 | 合作律所历史案例（脱敏） |
| Raw materials | 每案例5-10份文档 | 同上 |
| 参考答案 | 每案例1份 | 律师原始petition |

#### 脱敏处理

- 替换所有真实姓名、公司名、日期
- 使用虚构但合理的信息
- 确保案例仍然真实可信

---

## 四、论文结构

### 4.1 建议结构（约8000字）

**创新点篇幅分配（重要）：**

| 功能 | 篇幅占比 | 定位 |
|------|----------|------|
| Multi-stage Semantic Compression | 20% | 技术架构贡献 |
| Inline Provenance | 20% | 基础能力 |
| Many-to-One Citation | 20% | 技术贡献 |
| Gap Dashboard + What-if | **40%** | **核心创新，重点展开** |

> ⚠️ Inline Provenance（点击跳转）在HCI领域已有较多研究（ScholarPhi, Semantic Scholar等）。
> 必须把创新重心放在两个方面：
> 1. **Multi-stage Pipeline** — 语义压缩+溯源保留的技术架构
> 2. **Gap Dashboard的交互式模拟（Interactive Simulation）** — 律师可以通过模拟补充材料来预见AI的生成变化
> 
> 这才是UIST喜欢的Novel Interaction Technique。

```
1. Introduction (800字)
   - 问题：律师必须review AI输出，但现有工具不可追溯
   - 洞察：可追溯性可能改变review工作流
   - 贡献：系统 + 技术架构 + 交互设计 + 实证研究

2. Related Work (1500字)
   2.1 AI in Legal Document Preparation
   2.2 Explainable AI in High-Stakes Domains  
   2.3 Human-AI Collaboration in Professional Contexts
   2.4 Research Gap

3. Formative Study (800字) [可选]
   - 如果有前期访谈数据
   - 或者描述与律师合作的洞察

4. ImmigrationCraft System (2000字)
   4.1 Design Goals
   4.2 Multi-stage Pipeline Overview
       - 阶段0: OCR (BBox + Markdown双输出)
       - 阶段1: 语义分析与信息压缩
       - 阶段2: Evidence-Standard Mapping
       - 阶段3: Gap Analysis
       - 阶段4: Petition Generation
   4.3 Semantic Compression with Provenance Preservation
   4.4 Inline Provenance & Many-to-One Citation
   4.5 Evidence Gap Dashboard
   4.6 Lawyer Intervention Points (可视化交互调整)
   4.7 Implementation

5. User Study (1000字)
   5.1 Participants
   5.2 Study Design
   5.3 Task and Procedure
   5.4 Measures

6. Results (1500字)
   6.1 Task Performance (时间、准确率)
   6.2 Provenance Usage Patterns
   6.3 Confidence and Workload
   6.4 Qualitative Findings

7. Discussion (800字)
   7.1 Design Implications
   7.2 Limitations
   7.3 Future Work

8. Conclusion (300字)
```

### 4.2 关键论文语言

**Title选项：**

> A. ImmigrationCraft: Designing Verifiable AI-Assisted Legal Writing through Inline Provenance

> B. "I Need to See What the AI is Drawing From": Supporting Lawyer Review of AI-Generated Immigration Petitions

> C. From Black Box to Glass Box: Inline Provenance for Verifiable AI Legal Writing

**Abstract核心句（待填数据）：**

> "Through a between-subjects study with N immigration lawyers, we found that ImmigrationCraft users detected X% more errors in Y% less time compared to those using general-purpose LLMs, and reported significantly higher confidence in their review (M=X vs M=Y, p<.05)."

---

## 五、时间规划

### 5.0 目标会议

**UIST 2025**
- 预计截稿：2025年4月初（Abstract）/ 4月中旬（Full Paper）
- 内部Deadline：2025年3月15日（留buffer）
- 可用时间：约12周（2024年12月底 - 2025年3月中）

### 5.1 12周计划

| 周次 | 日期 | 任务 | 交付物 |
|------|------|------|--------|
| **Week 1-2** | 12/25-1/7 | 系统完善 + **图表OCR验证实验** | LLM OCR集成、多源引用功能、图表识别能力报告 |
| **Week 3** | 1/8-1/14 | 实验材料准备（骨架克隆法） | 两个等价L-1案例（脱敏）、实验protocol |
| **Week 4** | 1/15-1/21 | IRB申请（如需要） | IRB approval |
| **Week 5-6** | 1/22-2/4 | **Pilot Study** (2人) | 验证案例等价性、修正实验设计 |
| **Week 7-8** | 2/5-2/18 | **正式实验** (6-8人, Within-subjects) | 实验数据 |
| **Week 9** | 2/19-2/25 | 数据分析 | 统计结果、主题分析 |
| **Week 10-11** | 2/26-3/11 | 论文撰写 | 完整初稿 |
| **Week 12** | 3/12-3/15 | 修改润色 | 最终提交版 |

### 5.2 开发原则：不要造轮子

时间紧张，必须使用成熟组件库：

| 功能 | 推荐方案 | 避免 |
|------|----------|------|
| PDF渲染+高亮 | PDF.js + react-pdf | 自己写PDF解析 |
| Dashboard图表 | Recharts / ECharts | 自己用Canvas画 |
| UI组件 | Ant Design / shadcn/ui | 自己写组件 |
| 后端框架 | FastAPI | 复杂架构 |

**原则：能用现成的就用现成的，把时间花在核心功能上。**

### 5.2 关键里程碑

- [ ] **1月7日**：系统完成可用于实验的版本
- [ ] **1月21日**：所有实验材料准备完毕
- [ ] **2月4日**：Pilot study完成，实验设计确认
- [ ] **2月18日**：所有实验数据收集完毕
- [ ] **3月1日**：论文初稿完成
- [ ] **3月15日**：提交

---

## 六、风险与备选方案

### 6.1 潜在风险

| 风险 | 可能性 | 影响 | 应对 |
|------|--------|------|------|
| 律师招募困难 | 中 | 高 | 提前联系、提供报酬、利用合作关系 |
| 系统开发延迟 | 中 | 高 | 简化功能、先完成核心provenance |
| 实验效果不显著 | 中 | 高 | 增加质化分析权重、调整研究问题 |
| 真实案例无法使用 | 低 | 中 | 准备synthetic案例作为备选 |

### 6.2 备选方案

**如果between-subjects人数不够：**
- 改为within-subjects设计（每人两种条件，顺序平衡）
- 需要控制学习效应，使用不同案例

**如果定量结果不显著：**
- 强调质化发现
- 调整论文定位为"exploratory study"
- 重点讨论设计启示和未来研究方向

---

## 七、待确认事项

与同门/导师讨论后需确认：

1. [ ] Baseline确认：ChatGPT还是其他LLM？
2. [ ] 参与者人数：能招募多少律师？
3. [ ] 案例材料：能获取多少脱敏案例？
4. [ ] IRB：是否需要伦理审批？流程多久？
5. [ ] 系统开发：LLM OCR集成的时间表？
6. [ ] 论文分工：谁负责哪部分？

---

## 八、参考文献（核心）

### 必引文献

1. Swoopes et al. (2025). Lawyers' Attitudes Toward AI... [核心引用：律师验证需求]
2. Amershi et al. (2019). Guidelines for Human-AI Interaction. CHI.
3. Chalkidis et al. (2020). LEGAL-BERT. EMNLP.
4. Ribeiro et al. (2016). LIME. KDD.

### Sensemaking文献（建议补充）

ImmigrationCraft本质上是一个**意义构建（Sensemaking）**工具，帮助律师理解案情。建议引用：

5. Pirolli & Card (2005). The Sensemaking Process and Leverage Points for Analyst Technology. [经典Sensemaking模型]
6. Russell et al. (1993). The Cost Structure of Sensemaking. CHI. [Sensemaking成本分析]
7. Bae et al. (2022). Sensemaking in Legal Document Review. [法律领域Sensemaking]
8. Goyal & Howes (2019). Evidence Sensemaking in Intelligence Analysis. [证据分析]

### 相关系统文献

9. Head et al. (2021). ScholarPhi: A System for Entity-Based Reading. CHI. [学术文档溯源]
10. Chang et al. (2023). CiteSee: Augmenting Citations in Scientific Papers. CHI. [引用增强]

完整参考文献列表见：`reference_list_with_links.md`

---

## 附录A：实验Protocol详细流程

```
准备阶段（实验前）：
□ 确认参与者符合条件（移民法从业经验）
□ 发送informed consent
□ 确认实验时间（约60分钟）
□ 准备录屏软件、录音设备

实验当天：
□ 欢迎、介绍（5分钟）
□ 签署consent form
□ 随机分配条件（A或B）
□ 系统培训（5分钟）
□ 任务执行（30分钟）
  - 第一阶段：生成草稿（10分钟）
  - 第二阶段：Review修改（20分钟）
□ 问卷填写（5分钟）
  - 信心评分
  - NASA-TLX
□ 事后访谈（15分钟）
□ 发放报酬、感谢

实验后：
□ 保存所有数据（录屏、录音、日志、问卷）
□ 转录访谈
□ 编码分析
```

---

*本文档版本：v1.2*
*创建日期：2024年12月25日*
*最后更新：2024年12月30日 - 加入OCR与回溯系统具体实现*
*下次更新：与同门讨论后*
